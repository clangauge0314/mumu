PORT=3000
NODE_ENV=development

# 브라우저에서 이미지 URL을 절대 경로로 받을 때 (프로덕션 API 도메인)
# 비우면 /uploads/... 상대 경로 반환 (Vite 프록시·같은 오리진에 적합)
PUBLIC_BASE_URL=

# multipart 업로드 한 파일 최대 크기 (MB)
MAX_UPLOAD_MB=15

# CORS — 개발 시 Vite 기본 포트
CORS_ORIGIN=http://localhost:5173
